discord.py>=2.3
python-dotenv>=1.0
openai>=1.40
# Musik-Feature (Voice):
#   davey  = Discord-Voice-Verschluesselung (DAVE-Protokoll). Bei discord.py >= 2.7
#            ZWINGEND fuer Voice - ohne davey: "RuntimeError: davey library needed".
#   PyNaCl = aeltere Voice-Verschluesselung (Fallback, schadet nicht).
#   yt-dlp = YouTube-Audio.
# Zusaetzlich noetig (System-Paket, NICHT pip): ffmpeg  ->  z. B.  apt install ffmpeg
davey>=0.1.5
PyNaCl>=1.5
yt-dlp>=2024.8
# Spass-Features (Level/Coins, Mini-Games, Chaos): brauchen KEINE neuen Pakete
#   - Level & SigmaCoins speichern in data/economy.json (Standardbibliothek)
#   - Mini-Games & Chaos nutzen die schon vorhandene KI (openai) bzw. nur stdlib
#   - Soundboard: Audiodateien selbst in den Ordner sounds/ legen (mp3/wav/ogg)
# Leaderboard-Bild ('Flo top') im Grafana-Stil: braucht Pillow. Fehlt es, faellt
# der Bot automatisch auf die Text-/Embed-Bestenliste zurueck (kein Fehler).
Pillow>=10.0
# Voice-Gags TTS ('Flo sprich <text>') - eines von beiden, sonst ist nur TTS aus:
#   - espeak-ng = offline, System-Paket (NICHT pip):  apt install espeak-ng
#   - gTTS      = bessere Stimme, aber online + pip (optional, hier auskommentiert)
# gTTS>=2.5
